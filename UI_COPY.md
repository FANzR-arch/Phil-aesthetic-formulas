# Formula Lab 文案规范

界面以中文为主；英文只保留品牌、格式、技术名词和作品名称。不要在同一类按钮
之间混用“上传 / 导入”、“复位 / 重置”、“抽奖 / 随机”。

| 场景 | 固定写法 | 说明 |
| --- | --- | --- |
| 选择本地文件 | 导入图像 | 辅助信息写 `JPG / PNG` 或 `可多选` |
| 随机生成 | 随机配方 | 辅助信息说明随机范围 |
| 存储当前状态 | 保存配方 | 没有持久化能力时不使用“保存” |
| 回到起点 | 重置 | 辅助信息写“回到默认值”或“回到当前配方” |
| 输出文件 | 导出 PNG / 导出 MP4 | 格式写在主动作里，不用“下载”混写 |
| 输出文本 | 导出 TXT | 辅助说明写“文本文件”或具体编码 |
| 回画廊 | 全部实验 | 不用 `ALL PROJECTS`、`全部项目` 混用 |
| 空闲状态 | 就绪 | 可附格式、尺寸或本地处理状态 |

规则：主标签是动词加对象，尽量 2–4 个汉字；说明行补充格式、范围或结果，不重复主标签。
作品内部的专有视觉词（如 `ASCII Signal`、`WEBGL`、`RGB`）可以保留英文。
