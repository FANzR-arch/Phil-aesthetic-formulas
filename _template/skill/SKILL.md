---
name: {{skill-name-kebab-case}}
description: {{把什么输入编译成什么输出。用户提到「{{触发词1}}」「{{触发词2}}」……时使用。不用于{{边界}}。}}
---

# {{Skill 标题}}

## 目标

把 {{输入：主题/标题/照片……}} 编译成一条确定性的、可直接用于图像模型的成品提示词。

## 风格基座（每次输出都必须包含的不变量）

- {{构图/底色/质感/字体等硬规则}}

## 可变模块

- {{模块A：可选项……}}
- {{模块B：可选项……}}

## 编译流程

1. 识别用户意图，内部选定各模块（不向用户展示菜单）
2. 按「基座 + 选定模块 + 用户主题」拼装
3. 只输出一条成品提示词，不解释过程

## 输出格式

{{语言/长度/是否含比例参数等}}

## 不做什么

- {{反例与边界}}
